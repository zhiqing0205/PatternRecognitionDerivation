{
  "devDependencies": {
    "vitepress": "^1.6.3"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "export $(grep -v '^#' .env | xargs) && vitepress build",
    "docs:preview": "vitepress preview",
    "build:deploy": "export $(grep -v '^#' .env | xargs) && vitepress build"
  },
  "dependencies": {
    "markdown-it-mathjax3": "^4.3.2"
  }
}
